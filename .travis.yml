sudo: required

language: python

services:
    - docker

before_install:
    - docker pull quay.io/fenicsproject/stable:2016.1.0
    - docker run quay.io/fenicsproject/stable:2016.1.0 /bin/sh -c "dolfin-version"
    - docker ps -a
    
install:
    
script:
    - ls
    - docker run -t -i quay.io/fenicsproject/stable:2016.1.0 /bin/bash -c "dolfin-version"
